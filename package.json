{
  "name": "archimind",
  "version": "1.0.0",
  "description": "智能建筑知识图谱可视化平台",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "vis-network": "^9.1.9"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.17"
  }
}
